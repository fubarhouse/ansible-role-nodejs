---

- hosts: all
  sudo: true
  vars:
    ansible_ssh_user: root
    node_version: 6.2.2
    node_versions:
      - 6.2.2
    node_packages:
      - grunt
  roles:
    - { role: ../../fubarhouse.npm }
