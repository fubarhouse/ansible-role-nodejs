---

- hosts: all
  sudo: true
  vars:
    curl_version: 7.50.0
    node_version: 7.0.0
    node_versions:
      - 6.0.0
    node_packages:
      - grunt
  roles:
    - geerlingguy.git
    - fubarhouse.curl
    - role_under_test
