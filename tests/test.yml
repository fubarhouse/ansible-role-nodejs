---

- hosts: all
  sudo: true
  vars:
    ansible_ssh_user: root
    node_version: 7.0.0
    node_versions:
      - 6.0.0
    node_packages:
      - grunt
  roles:
    - andrewrothstein.curl
    - geerlingguy.git
    - role_under_test
