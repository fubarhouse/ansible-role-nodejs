---

- hosts: localhost
  sudo: true
  vars:
    node_version: 7.0.0
    node_versions:
      - 6.0.0
    node_packages:
      - grunt
  roles:
    - geerlingguy.git
    - fubarhouse.curl
    - role_under_test
