---

# default user name
vagrant_user: vagrant

# default node application versions
default_nvm_version: 0.26.1
default_node_version: 4.0.0
default_io_version: 3.3.1

# packages to install
npm_packages:
  - { name: backbone }
  - { name: grunt }
  - { name: underscore }
  - { name: yosay }
